'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.print = exports.parse = exports.AST = undefined;

var _Parser = require('./Parser.js');

var _Printer = require('./Printer.js');

var _ScopeResolver = require('./ScopeResolver.js');

var _AST = require('./AST.js');

var AST = _interopRequireWildcard(_AST);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

exports.AST = AST;
exports.parse = parse;
exports.print = print;


function addParentLinks(node) {
  node.children().forEach(child => {
    child.parent = node;
    addParentLinks(child);
  });
}

function print(ast) {
  return new _Printer.Printer().print(ast);
}

function parse(input, options) {
  options = options || {};

  let parser = new _Parser.Parser(input, options);
  let result = options.module ? parser.parseModule() : parser.parseScript();

  if (options.resolveScopes) result.scopeTree = new _ScopeResolver.ScopeResolver().resolve(result);

  if (options.addParentLinks) addParentLinks(result.ast);

  return result;
}