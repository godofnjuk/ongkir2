doctype html
html
  head
    title= title
    script(src='//static.parastorage.com/services/js-sdk/1.537.0/js/wix.min.js')
    script(type="text/javascript")
      function local (arr) 
          var i;
          i = "#{result}"
          localStorage.setItem('xyz', i)
    
      console.log('stored')
      local()
  body